<% layout('/layout/layout') -%>
<h1><%= note.title %></h1>
<h5> Written by <%= author%></h5>
<p><%= note.body %></p>
<hr>
<% if(user){ %>
  <% if(user.id ==note.author) { %>
    <a class="btn btn-default" href='/notes/edit/<%=note._id%>' >Edit </a>
		<a class="btn btn-danger delete-note" href='#' data-id="<%= note._id %>" > Delete </a>
  <% } %>
<% } %>